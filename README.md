# generate-mikes-resume
Generate HTML or JSON representations of my resume.

## Install
```bash
go install github.com/mikerybka/generate-mikes-resume@latest
```

## Usage
```bash
generate-mikes-resume [html|json]
```
